aiohttp>=3.9
gpiozero>=2.0
sinricpro>=6.0.0,<7
# lgpio, pyserial (serial) e smbus vem dos pacotes do sistema (python3-lgpio,
# python3-serial, python3-smbus via apt), nao daqui - o venv precisa ser
# criado com --system-site-packages para enxergar. pyserial e smbus sao
# usados só pelo configurar_sensor.py (ajuste do C4001 via UART).
